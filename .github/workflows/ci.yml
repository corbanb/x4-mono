name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Type Check, Lint, Test
    runs-on: ubuntu-latest
    # TODO: Uncomment steps after PRD-001 (Monorepo Foundation) is implemented.
    # This workflow is a placeholder that validates the CI structure exists.
    # Full CI pipeline is defined in PRD-014.
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # - name: Setup Bun
      #   uses: oven-sh/setup-bun@v2
      #   with:
      #     bun-version: latest

      # - name: Install dependencies
      #   run: bun install --frozen-lockfile

      # - name: Type check
      #   run: bun turbo type-check

      # - name: Lint
      #   run: bun turbo lint

      # - name: Test
      #   run: bun turbo test

      - name: Placeholder
        run: echo "CI pipeline placeholder — activate after PRD-001 implementation"
